# Daneeqx Dev Container

Кастомный Dev Container с настроенным окружением для разработки на Go и работы с Protobuf.

## 🚀 Возможности

- **Go 1.25** - полная поддержка разработки на Go
- **Protobuf** - инструменты для работы с Protocol Buffers
- **Docker-in-Docker** - возможность запускать Docker внутри контейнера
- **Git** - полный набор расширений для работы с Git
- **SSH** - автоматический проброс SSH ключей с хоста
- **ZSH** - настроенный Zsh как шелл по умолчанию

## 📦 Установленные компоненты

### Базовые инструменты
- Ubuntu 24.04
- Common utilities (via devcontainers/features/common-utils)
- ZSH как default shell

### Go окружение
- Go 1.25
- GOPRIVATE

### VS Code расширения

**Git:**
- GitHistory
- .gitignore support
- Git Graph
- Git Blame
- GitLens

**Разработка:**
- GitHub Copilot
- Rainbow CSV
- Protobuf (pbkit.vscode-pbkit)

## 🛠️ Структура features

Проект использует модульную систему features:

- **base** - базовые пакеты и настройки
- **go** - Go toolchain и конфигурация
- **protobuf** - инструменты для работы с Protobuf
- **ssh** - проброс SSH ключей
- **git** - Git и расширения для VS Code
- **dind** - Docker-in-Docker

## 📋 Использование

### Открытие проекта в Dev Container

1. Откройте проект в VS Code
2. Нажмите `CMD+Shift+P` и выберите `Dev Containers: Reopen in Container`
3. Дождитесь сборки и запуска контейнера

### SSH Keys

SSH ключи автоматически монтируются из `~/.ssh` хоста в `/home/vscode/.ssh` контейнера.

### Git конфигурация

Git настроен и готов к использованию с полным набором расширений для удобной работы.

### Docker

Docker доступен внутри контейнера благодаря Docker-in-Docker feature.

## ⚙️ Конфигурация

Основная конфигурация находится в `.devcontainer/devcontainer.json`.

Каждая feature имеет свой файл конфигурации в `.devcontainer/features/<feature-name>/devcontainer-feature.json`.

## 🔧 Настройка окружения

### GOPRIVATE

Для работы с приватными Go модулями настроена переменная:
```bash
GOPRIVATE=gitlab.com/*
```

Также используется `NETRC=/etc/.netrc` для аутентификации.

## 📝 Примечания

- Пользователь в контейнере: `vscode`
- Телеметрия VS Code отключена
- Базовый образ: `mcr.microsoft.com/devcontainers/base:dev-ubuntu-24.04`

## 🤝 Поддержка

При возникновении проблем с Dev Container:
1. Проверьте логи сборки
2. Убедитесь, что Docker Desktop запущен
3. Попробуйте пересобрать контейнер: `Dev Containers: Rebuild Container`